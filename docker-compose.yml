services:
  claude:
    build: 
      context: ./build_files
      dockerfile: Dockerfile
    container_name: claude-dev
    user: dev
    stdin_open: true
    tty: true
    working_dir: /workspace
    networks: [devnet]
    volumes:
      - asdf_data:/home/dev/.asdf
      - claude_data:/home/dev/.claude
      - ./.claudinho:/home/dev/.claudinho
      - ./workspace:/workspace
volumes:
  asdf_data:
  claude_data:
    external: true
networks:
  devnet:
    driver: bridge